@host = https://localhost:5001
@token = your-jwt-token-here

### Login with Active Directory credentials
POST {{host}}/api/auth/login
Content-Type: application/json

{
  "username": "your-ad-username",
  "password": "your-password"
}
###

### Check authentication status
GET {{host}}/api/auth/status
###

### List all files
GET {{host}}/api/files
###

### List all files (with authentication)
GET {{host}}/api/files
Authorization: Bearer {{token}}
###

### Upload a file (without authentication)
POST {{host}}/api/files
Content-Type: multipart/form-data; boundary=boundary

--boundary
Content-Disposition: form-data; name="file"; filename="test.txt"
Content-Type: text/plain

This is a test file content.
--boundary--
###

### Upload a file (with authentication)
POST {{host}}/api/files
Authorization: Bearer {{token}}
Content-Type: multipart/form-data; boundary=boundary

--boundary
Content-Disposition: form-data; name="file"; filename="test.txt"
Content-Type: text/plain

This is a test file content.
--boundary--
###

### Get file metadata
GET {{host}}/api/files/{fileName}/metadata
###

### Download a file
GET {{host}}/api/files/{fileName}
###

### Delete a file
DELETE {{host}}/api/files/{fileName}
###

### Delete a file (with authentication)
DELETE {{host}}/api/files/{fileName}
Authorization: Bearer {{token}}
###
